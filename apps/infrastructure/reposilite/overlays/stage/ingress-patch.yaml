apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reposilite
spec:
  tls:
  - hosts:
    - reposilite-stage.taylor.lan
    secretName: reposilite-stage-tls
  rules:
  - host: reposilite-stage.taylor.lan